# Keep the browser API path same-origin.
NEXT_PUBLIC_API_BASE_URL=/api-backend

# Set this on Vercel to your deployed backend URL.
BACKEND_URL=https://your-backend.onrender.com

# Optional: enables the real Naver map on the live meeting page.
NEXT_PUBLIC_NAVER_MAPS_CLIENT_ID=
